describe('responsive-carousel directive with config', function() {
  'use strict';

  var provider;

  beforeEach(module('angularResponsiveCarousel', function(angularResponsiveCarouselProvider) {
    provider = angularResponsiveCarouselProvider;
  }));


});
